@{
    ViewBag.Title = "Login";
}
<head>

    <script>
        function onSignIn(googleUser) {
            var profile = googleUser.getBasicProfile();
            console.log('ID: ' + profile.getId()); // Do not send to your backend! Use an ID token instead.
            console.log('Name: ' + profile.getName());
            console.log('Image URL: ' + profile.getImageUrl());
            console.log('Email: ' + profile.getEmail()); // This is null if the 'email' scope is not present.
            myBitch();
            window.location.replace("~/LandingPage/Index")

            //$.get("/LandingPage/Index", function (data) {
            //    profile;
            //})
        }
        function myBitch(profile) {
            if (profile == null) {

            }
            else {
                window.location.replace("~/LandingPage/Index")
            }
        }
       

    </script>
    <meta name="google-signin-client_id" content="471390001196-1nk4btnrv8bkstb1qr3npl9cco5nvdpi.apps.googleusercontent.com">
</head>

@using (Html.BeginForm("Login", "LandingPage", FormMethod.Post))
{
    <label for="inputEmail">Email address</label>
    @Html.TextBox("Email address")
    <label for="inputPassword">Password</label>
    @Html.Password("Password")
    <button type="submit">Login</button>
}

    <a href="@Url.Action("Index", "Home")w"><div class="g-signin2" data-onsuccess="onSignIn"></div></a>
<a href="/LandingPage/Logout" onclick="signOut();">Sign out</a>  